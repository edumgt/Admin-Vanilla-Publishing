apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-vanilla-config
data:
  APP_ENV: production
  BACKEND_PORT: "8000"
  FRONTEND_PORT: "80"
  CORS_ALLOW_ORIGINS: https://app.example.com
  DATABASE_NAME: newhomepage
  DATABASE_HOST: postgres
  DATABASE_PORT: "5432"
